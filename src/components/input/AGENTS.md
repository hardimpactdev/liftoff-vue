# Input

## What is this component?

The Input component displays a form input field. It functions as a standard text field but includes styling consistent with the design system.

## When to use this component?

- To capture text input from the user (e.g., email, password, search query).

## Implementation Details

- **Input**: The root component.
